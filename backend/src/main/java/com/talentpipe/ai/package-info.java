/**
 * AI module — resume parsing, candidate scoring and semantic matching
 * (pgvector-backed; the {@code vector} extension is already enabled in V1).
 *
 * <p>Placeholder only: implemented in a later sprint. Module boundary exists
 * from day one; modules communicate via services/DTOs, never entities.</p>
 */
package com.talentpipe.ai;
