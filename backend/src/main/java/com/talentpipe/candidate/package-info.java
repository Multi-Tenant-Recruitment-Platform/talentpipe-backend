/**
 * Candidate module — candidate profiles, resumes and candidate-side accounts.
 *
 * <p>Placeholder only: no code lands here until the Candidate sprint.
 * Candidates are tenant-independent and will NOT reuse the auth module's
 * {@code users} table (see the architecture doc). Module boundary exists from
 * day one so nothing else grows into this space.</p>
 */
package com.talentpipe.candidate;
